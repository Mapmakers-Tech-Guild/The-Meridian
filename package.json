{
  "name": "mapmakers-kb-tooling",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "postinstall": "node scripts/vend-quartz.mjs && node scripts/ensure-quartz-cache-link.mjs",
    "build:graph": "node scripts/build-kb-graph.mjs",
    "build:site": "node scripts/vend-quartz.mjs && node scripts/ensure-quartz-cache-link.mjs && npm run build:graph && npx quartz build -d . -o public && node scripts/collect-meridian-assets.mjs",
    "test:build": "node scripts/test-build.mjs"
  },
  "devDependencies": {
    "@jackyzha0/quartz": "github:jackyzha0/quartz#v4",
    "d3": "^7.9.0"
  }
}
